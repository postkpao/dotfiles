super + b
    firefox
super + space
    xfce4-appfinder
control + alt + e
    thunar
control + shift + Escape
    kitty -e htop
super + e
    kitty -T ranger -e ranger 
super + m
    (sleep 0.1 & kitty -T clyrics -e clyrics -c) & (sleep 0.2 && kitty -T cmus -e cmus)
super + c
    cmus-remote --pause
super + n
    cmus-remote --next
super + p
    cmus-remote --prev
super + s
    cmus-remote --shuffle
super + v
    cmus-remote --stop && cmus-remote --play
control + shift + @Print
    ~/scripts/PicToTextClip.sh
shift + @Print
    escrotum -s
@Print
    ~/scripts/anki/print.sh
control + alt + t
    kitty -T kitty_to_tag
super + k
    passmenu
alt + shift + a
    setxkbmap ma
alt + shift + d
    setxkbmap de
super + F12
    slock
control + alt + r
    pkill redshift
control + alt + shift + r
    redshift -l 49.00937:8.40444 &
control + alt + shift + q
    kill -9 -1
